(function () {
    "use strict";

    function userRoomService($cookies, $http, $rootScope, $q) {
      
    };


    angular
        .module("Web.Services")
        .service("userRoomService", ["$cookies", "$http", "$rootScope", "$q", userRoomService]);
})();